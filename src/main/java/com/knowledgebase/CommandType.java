package com.knowledgebase;

/**
 * Enum representing the types of commands supported by the CLI.
 * 
 * Requirements: 4.3, 8.1
 */
public enum CommandType {
    CREATE,
    READ,
    UPDATE,
    DELETE,
    LIST,
    SEARCH,
    LINKS,
    EXIT,
    HELP
}
